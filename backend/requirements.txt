fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
python-dotenv>=1.0

# Supabase is reached over its PostgREST HTTP API, as the signed-in user.
# No supabase client library is needed, and the service-role key is never used.
httpx>=0.27

anthropic>=1.0

pytest>=8.0
